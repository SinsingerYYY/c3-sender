# C3-sender-IDF5 项目需求说明

> ESP32-C3 · UART ↔ ESP-NOW · RoboMaster 协议帧感知桥

## 一、目标与平台

- **开发环境**：PlatformIO + Arduino framework。
- **硬件**：ESP32-C3（C3 SuperMini；板载 LED 在 GPIO8，低电平点亮）。
- **通信方式**：ESP-NOW 点对点，固定对端 MAC。
- **功能**：UART ↔ ESP-NOW 双向桥，按 RoboMaster 协议整帧转发。
- **工程目录**：`/home/singer/esp/C3-sender-IDF5`
  - 已升级 **Arduino core3 / ESP-IDF5**，使用官方 **`ESP32_NOW_Serial`**。
  - ESP-NOW v2，`ESP_NOW_MAX_DATA_LEN_V2 = 1470`。
  - 原 IDF4.4 工程 `C3-sender` 保留作对照。

## 二、串口配置

| 项目 | 值 |
| --- | --- |
| 业务串口 | UART0 |
| GPIO20 | ESP32 RX ← 主板 TX |
| GPIO21 | ESP32 TX → 主板 RX |
| 波特率 | 921600 |
| 格式 | 8N1（8 数据位、1 停止位、无校验、无硬件流控） |
| USB Serial | 仅输出日志，不承载业务数据 |

## 三、RM 协议帧处理（核心）

按 **RoboMaster 2026 机甲大师高校系列赛通信协议 V2.0.0** 切帧。

### 帧结构

```text
frame_header (5B) + cmd_id (2B) + data (nB) + frame_tail (2B)
```

| 域 | 偏移 | 大小 | 说明 |
| --- | --- | --- | --- |
| SOF | 0 | 1 | 固定 0xA5 |
| data_length | 1 | 2 | data 字段长度（小端） |
| seq | 3 | 1 | 包序号 |
| CRC8 | 4 | 1 | 帧头校验（本桥不校验） |
| cmd_id | 5 | 2 | 命令码 |
| data | 7 | n | 数据 |
| frame_tail | 7+n | 2 | CRC16（本桥不校验） |

**整帧长度 = 9 + data_length**

### 处理规则

- 以 `SOF = 0xA5` 作为帧起始；读 5 字节帧头取 `data_length`。
- **不校验 CRC8、不校验 CRC16**（有效性由应用端识别）。
- **所有长度都转发**（实际不会超过 `MAX_FRAME = 1470`）。
- 收到完整帧 → **整帧一次发送**。
- 接收侧同样按 RM 帧切分，**整帧一次输出**到 UART。
- 非帧字节：丢弃并重同步（从下一个 `0xA5` 重新找）。
- 长度非法（`total < 9` 或 `total > 1470`）：丢弃当前、重同步。

## 四、实时性与可靠性

- **不做应用层 ACK、不重传**，仅依赖 ESP-NOW 链路级（物理层 802.11 ACK）。
- **不缓存**（避免命令缓存导致延迟）：
  - 发送侧缓冲不足（`availableForWrite < 整帧长`）或写入不足 → **丢弃该整帧**并计数。
- 面向实时场景，新数据优先，不做长时间排队。

## 五、预期负载

| 数据帧 | data 长度 | 整帧长度 | 频率 | 方向 |
| --- | --- | --- | --- | --- |
| 大帧 | 300 B | 309 B | 50 Hz（≈15.5 KB/s） | 上行/下行之一 |
| 小帧 | 75 B | 84 B | 35 Hz（≈2.9 KB/s） | 另一方向 |

- 两路为上行/下行关系，不同时发送（需留保护）。
- 921600（≈92 KB/s）带宽余量充足。

## 六、两块板配置

两块板烧录**同一份代码**，只修改 `PEER_MAC` 为对方，切勿指向自己。

| 板 | 本机 MAC | `PEER_MAC` 填 |
| --- | --- | --- |
| 无柱（发送端） | `70:AF:09:3B:0F:40` | `70:AF:09:3B:1F:E4` |
| 有柱（接收端） | `70:AF:09:3B:1F:E4` | `70:AF:09:3B:0F:40` |

公共配置：

```text
CHANNEL = 1
UART_BAUD = 921600
```

## 七、日志与指示灯

- **启动日志**：本机 MAC、对端 MAC、信道、UART 参数、`ESP-NOW v2 / maxDataLen`、RM 帧格式。
- **运行统计**：每秒一行
  ```text
  [stat] TX frames=.. drop=.. bytes=.. | RX frames=.. bytes=..
  ```
- **逐帧 hex 调试**：默认关闭（`DEBUG_TX = false`、`DEBUG_RX = false`），防止 USB 输出拖垮 921600 串口。
- **LED（GPIO8，低电平点亮）**：
  - 收到任一方向帧后 5 秒内 → 1Hz 闪烁；
  - 空闲（5 秒无数据）→ 每 5 秒短闪一次。

## 八、代码结构约定

- 业务代码全部放在 `src/main.cpp`。
- 不确定的数值统一放在文件头部常量区。
- 依赖：Arduino 内置库、ESP-NOW 内置库（官方 `ESP32_NOW_Serial`）。

## 九、构建与烧录

```bash
cd /home/singer/esp/C3-sender-IDF5
~/.platformio/penv/bin/platformio run -e esp32-c3-devkitm-1
~/.platformio/penv/bin/platformio run -e esp32-c3-devkitm-1 -t upload
```

平台配置（`platformio.ini`）：

```ini
platform = https://github.com/pioarduino/platform-espressif32/releases/download/55.03.311/platform-espressif32.zip
build_flags =
    -DARDUINO_USB_MODE=1
    -DARDUINO_USB_CDC_ON_BOOT=1
```

## 十、验收要点

1. 启动日志显示 `ESP-NOW v2, maxDataLen=1470`。
2. 两板互指后能稳定收发 RM 帧。
3. 300B 数据帧 @50Hz 下，接收端 `RX frames` 稳定增长、发送端 `drop` 保持 0。
4. 921600 波特率下 GPIO20/21 链路稳定。
5. 帧切分正确：非法/残帧不输出，完整帧整帧输出。
